# Телефон как Веб-камера 📱➡️💻

Проект, позволяющий использовать камеру Android-телефона в качестве веб-камеры для компьютера под управлением Windows. Соединение возможно как по Wi-Fi, так и через USB (с использованием ADB).

На компьютере создается виртуальное устройство веб-камеры, которое можно использовать в большинстве приложений (OBS Studio, Zoom, Skype, Discord, браузеры и т.д.).

**Технологии:**
*   **Android Приложение:** Kotlin, CameraX
*   **Компьютерное Приложение:** Python, PySide6 (GUI), OpenCV, PyVirtualCam

## ✨ Возможности

*   Передача видеопотока с камеры телефона на ПК.
*   Подключение по локальной сети Wi-Fi.
*   Подключение через USB-кабель (ADB port forwarding).
*   Создание виртуальной веб-камеры на ПК ("OBS Virtual Camera").
*   Удаленное переключение между передней и задней камерами телефона с помощью GUI на ПК.
*   Графический интерфейс (GUI) на ПК для управления подключением и камерой.
*   Окно предпросмотра видеопотока в GUI на ПК.

## 📂 Структура Проекта

```
.
├── PhoneAsCamera_Server/     # Код Python-клиента для ПК (с GUI)
│   └── webcam_client_gui.py  # Основной скрипт клиента
├── PhoneAsWebcam/            # Исходный код Android-приложения
│   ├── app/                  # Основной модуль приложения
│   ├── build.gradle
│   ├── gradle/
│   └── ... (стандартная структура Android-проекта)
├── .gitignore                # Файл для Git
└── README.md                 # Этот файл
```

*   **`PhoneAsWebcam/`**: Папка содержит полный проект Android-приложения. Открывать эту папку в Android Studio.
*   **`PhoneAsCamera_Server/`**: Папка содержит Python-скрипт для компьютера. Запускать скрипт из этой папки.

## ⚙️ Необходимые компоненты (Пререквизиты)

Перед началом установки убедитесь, что у вас есть:

**Для Телефона (Android):**
*   Android телефон с версией **Android 5.0 (Lollipop, API 21)** или новее.
*   Включенный **Режим разработчика** (Developer Options).
*   Включенная **Отладка по USB** (USB Debugging) в настройках разработчика (потребуется для установки приложения и для USB-режима).

**Для Компьютера (Windows):**
*   **Windows 10** или **Windows 11**.
*   **Python 3.x** (рекомендуется 3.8 или новее, **обязательно 64-битная версия**). При установке Python **установите галочку "Add Python to PATH"**. Скачать можно с [python.org](https://www.python.org/).
*   **pip** (обычно устанавливается вместе с Python).
*   **ADB** (Android Debug Bridge). Инструкция по установке ниже.
*   **OBS Studio** (даже если вы не планируете стримить). Нам нужен компонент **OBS Virtual Camera**, который устанавливается вместе с OBS. Скачать с [obsproject.com](https://obsproject.com/). После установки OBS **перезагрузите компьютер**.

## 🛠️ Установка

Процесс установки состоит из двух частей: настройка Android-приложения и настройка компьютерного клиента.

### Часть 1: Android Приложение (`PhoneAsWebcam`)

Есть два основных способа установить приложение на телефон:

**Способ А: Сборка и установка из Android Studio (Рекомендуется, если есть Android Studio)**

1.  **Клонировать репозиторий:** Если вы еще не скачали проект, сделайте это с помощью Git:
    ```bash
    git clone <URL вашего репозитория>
    cd <папка репозитория>
    ```
2.  **Открыть проект:** Запустите Android Studio. Выберите "Open" (или "File" -> "Open") и укажите путь к папке `PhoneAsWebcam` внутри скачанного репозитория.
3.  **Синхронизация Gradle:** Дождитесь, пока Android Studio синхронизирует проект и скачает все необходимые зависимости (это может занять некоторое время). Если появятся ошибки или предложения установить недостающие SDK/Build Tools, согласитесь.
4.  **Подключить телефон:** Подключите ваш Android-телефон к компьютеру с помощью USB-кабеля. Убедитесь, что на телефоне включена отладка по USB и вы разрешили отладку с вашего компьютера (на телефоне появится всплывающее окно).
5.  **Запустить приложение:** В Android Studio выберите ваш телефон в списке устройств (обычно вверху) и нажмите зеленую кнопку "Run 'app'" (значок ▶️). Android Studio соберет приложение и установит его на ваш телефон.
6.  **Предоставить разрешения:** При первом запуске приложение запросит разрешение на использование **Камеры** и доступ к **состоянию Wi-Fi**. Обязательно предоставьте эти разрешения.

**Способ Б: Установка готового APK (Если нет Android Studio или для простоты)**

*(Предполагается, что APK-файл уже собран и доступен)*

1.  **Собрать APK (если нужно):** Если у вас есть Android Studio, но вы хотите просто APK: Build -> Build Bundle(s) / APK(s) -> Build APK(s). Готовый файл будет находиться в папке `PhoneAsWebcam/app/build/outputs/apk/debug/` (файл `app-debug.apk`).
2.  **Скопировать APK на телефон:** Подключите телефон к компьютеру по USB и скопируйте файл `.apk` в память телефона.
3.  **Установить APK:** На телефоне найдите скопированный файл `.apk` с помощью файлового менеджера. Нажмите на него. Возможно, потребуется разрешить установку из неизвестных источников в настройках безопасности Android. Установите приложение.
4.  **Предоставить разрешения:** При первом запуске приложение запросит разрешение на использование **Камеры** и доступ к **состоянию Wi-Fi**. Обязательно предоставьте эти разрешения.

### Часть 2: Компьютерное Приложение (`PhoneAsCamera_Server`)

1.  **Установить Python:** Если еще не установлен, скачайте 64-битную версию с [python.org](https://www.python.org/) и установите. **Не забудьте поставить галочку "Add Python to PATH"** во время установки. Проверить установку можно, открыв командную строку (cmd) и введя `python --version`.
2.  **Установить OBS Studio:** Скачайте и установите OBS Studio с [obsproject.com](https://obsproject.com/). Убедитесь, что компонент **OBS Virtual Camera** выбран при установке (обычно по умолчанию). **Перезагрузите компьютер** после установки.
3.  **Установить ADB:**
    *   Скачайте **SDK Platform Tools for Windows** с официальной страницы: [developer.android.com/tools/releases/platform-tools](https://developer.android.com/tools/releases/platform-tools)
    *   Создайте папку, например, `C:\platform-tools`.
    *   Распакуйте содержимое скачанного ZIP-архива в эту папку (там будет `adb.exe` и другие файлы).
    *   Добавьте путь к этой папке (например, `C:\platform-tools`) в системную переменную среды `PATH`. (Инструкция: Нажмите Win, введите "Переменные среды", выберите "Изменение системных переменных среды" -> "Переменные среды..." -> В "Системные переменные" найдите `Path`, нажмите "Изменить..." -> "Создать" -> Вставьте путь -> ОК везде).
    *   Откройте **новую** командную строку и проверьте командой `adb version`. Должна отобразиться версия ADB.
4.  **Установить библиотеки Python:**
    *   Откройте командную строку (cmd) или терминал.
    *   Перейдите в папку с Python-скриптом:
        ```bash
        cd path\to\your\repository\PhoneAsCamera_Server
        ```
        *(Замените `path\to\your\repository` на реальный путь)*
    *   Выполните команду для установки необходимых библиотек:
        ```bash
        pip install PySide6 opencv-python numpy "pyvirtualcam[obs]"
        ```

## ▶️ Использование

После того как обе части установлены, можно запускать систему.

1.  **Подготовка:**
    *   **Для режима Wi-Fi:** Убедитесь, что телефон и компьютер подключены к **одной и той же сети Wi-Fi** (точка доступа на компьютере-сервере или телефоне-клиенте **НЕ СЧИТАЕТСЯ!!!**).
    *   **Для режима USB:** Подключите телефон к компьютеру по USB. Убедитесь, что включена **Отладка по USB** и компьютер авторизован для отладки.

2.  **Запуск на Телефоне:**
    *   Запустите установленное приложение "PhoneAsWebcam".
    *   Используйте переключатель внизу, чтобы выбрать нужный режим: **Wi-Fi** или **USB**.
    *   Если выбран режим **Wi-Fi**, приложение покажет текущий IP-адрес телефона в сети. Запомните или запишите его.
    *   Нажмите кнопку **"Старт"**. Статус изменится на "Ожидание...".

3.  **Настройка ADB (Только для режима USB):**
    *   Откройте командную строку на компьютере.
    *   Проверьте, видит ли ADB ваш телефон:
        ```bash
        adb devices
        ```
        Вы должны увидеть серийный номер устройства и статус `device`.
    *   Выполните команду для проброса порта (порт 8888 используется по умолчанию в коде):
        ```bash
        adb forward tcp:8888 tcp:8888
        ```
        Эта команда создает туннель: запросы на порт 8888 компьютера будут перенаправляться на порт 8888 телефона через USB. **Эту команду нужно выполнять каждый раз перед использованием USB-режима**, пока телефон подключен.

4.  **Запуск на Компьютере:**
    *   Откройте командную строку (если еще не открыта).
    *   Перейдите в папку с Python-скриптом:
        ```bash
        cd path\to\your\repository\PhoneAsCamera_Server
        ```
    *   Запустите GUI-клиент:
        ```bash
        python webcam_client_gui.py
        ```

5.  **Использование GUI на Компьютере:**
    *   В появившемся окне выберите **тот же режим подключения** (Wi-Fi или USB), который вы выбрали на телефоне.
    *   Если вы выбрали **Wi-Fi**, введите IP-адрес телефона (который вы видели в приложении) в поле "IP Адрес Телефона".
    *   Нажмите кнопку **"Подключиться"**.
    *   В строке статуса должно появиться сообщение "Подключено...", а в окне превью - изображение с камеры телефона.

6.  **Проверка Веб-камеры:**
    *   Откройте любое приложение, использующее веб-камеру (например, встроенное приложение "Камера" Windows, OBS Studio, Zoom, Discord, сайт webcamtests.com).
    *   В настройках источника видео этого приложения выберите камеру с именем **"OBS Virtual Camera"**. Вы должны увидеть видео с вашего телефона.

7.  **Управление:**
    *   Кнопка **"Переключить Камеру"** в GUI на компьютере позволяет переключаться между передней и задней камерами телефона.
    *   Кнопка **"Отключиться"** в GUI разрывает соединение.
    *   Для полного завершения остановите сервер на телефоне (кнопка "Стоп") и закройте окно GUI на компьютере.

## ⚠️ Возможные Проблемы (Troubleshooting)

*   **Connection refused / Не удалось подключиться:**
    *   Проверьте, что выбран **одинаковый режим** (Wi-Fi/USB) на телефоне и в GUI.
    *   **Wi-Fi:** Убедитесь, что введен правильный IP-адрес. Проверьте, что телефон и ПК в одной сети. Брандмауэр/антивирус может блокировать соединение (попробуйте временно отключить для теста).
    *   **USB:** Убедитесь, что выполнена команда `adb forward tcp:8888 tcp:8888`. Проверьте `adb devices`, что телефон виден и авторизован. Убедитесь, что на телефоне выбран режим USB и нажат "Старт".
*   **Нет виртуальной камеры "OBS Virtual Camera" в списке:** Убедитесь, что OBS Studio установлен корректно и вы перезагрузили компьютер после установки.
*   **Не отображается IP-адрес на телефоне (в режиме Wi-Fi):** Убедитесь, что Wi-Fi на телефоне включен и подключен к сети. Проверьте, что приложению дано разрешение "Доступ к состоянию Wi-Fi".
*   **Видео тормозит / задерживается:** Качество Wi-Fi соединения сильно влияет на производительность. Попробуйте подойти ближе к роутеру. В режиме USB задержка обычно меньше.
*   **Ошибка "Insufficient memory" в Python:** Убедитесь, что вы используете **64-битную** версию Python. Закройте другие ресурсоемкие приложения. Перезагрузите компьютер.

## Лицензия

Этот проект распространяется под лицензией MIT. Подробности см. в файле `LICENSE` (если он есть).